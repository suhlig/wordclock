#!/usr/bin/env ruby
# frozen_string_literal: true

$LOAD_PATH.unshift File.expand_path('../../lib', __FILE__)

(0..23).each do |hour|
  (0..59).each do |minute|
    `bundle exec bin/word-clock #{hour} #{minute}`
    sleep 0.5
  end
end
